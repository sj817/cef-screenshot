# cef-screenshot-win32-x64-msvc

`cef-screenshot` 的 Windows x64 MSVC 原生绑定（`.node` 文件）。

此包由 `cef-screenshot` 主包作为 `optionalDependencies` 自动安装，**请勿直接安装**。

## 系统要求

- **操作系统**: Windows x64
- **运行时**: Node.js >= 22.6.0
